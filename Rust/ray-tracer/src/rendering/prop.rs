/// A structure that holds coordinates.
pub struct Point {
    pub x: f64,
    pub y: f64,
    pub z: f64,
}

/// A color struct containing rgba values.
pub struct Color {
    pub r: u8,
    pub g: u8,
    pub b: u8,
    pub a: u8
}
